{
  "name": "your-project",
  "version": "1.0.0",
  "scripts": {
    "server": "cd server && yarn start",
    "client": "cd client && yarn start",
    "start": "concurrently \"yarn server\" \"yarn client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.0"
  }
}
